Отчёт по практической работе №7
Выполнила: Григорьева Карина Алексеевна
Цель: освоить базовые операции работы с системой контроля версий Git и платформой GitHub, включая создание репозитория, управление ветками, коммиты, пуш и деплой статического сайта.
Ход работы.
Шаг 1. Установка и настройка Git

Открыла Терминал через Spotlight.

Проверила наличие Git командой git --version — система показала версию, значит, Git уже установлен.

Настроила Git: указала своё имя и email через команды git config --global user.name и git config --global user.email.

Проверила настройки командой git config --list — данные отобразились корректно.

Шаг 2. Создание локального репозитория

Создала папку проекта homework-iit командой mkdir и перешла в неё командой cd.

Инициализировала репозиторий командой git init — система подтвердила создание.

Проверила наличие папки .git командой ls -a — папка обнаружена.

Шаг 3. Создание и добавление отчёта

Создала файл report.md командой touch.

Открыла файл в TextEdit командой open -a TextEdit.

Заполнила отчёт заголовком и своими данными.

Добавила файл в индекс командой git add report.md.

Сделала первый коммит командой git commit -m "Initial commit: создан отчёт" — изменения зафиксированы.